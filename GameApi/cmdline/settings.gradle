pluginManagement {
    repositories {
        mavenLocal()
        maven { url new File(settingsDir, 'm2/repository').toURI() }
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'gameapi_cmdline'
